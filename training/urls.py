from django.urls import path
from . import views

app_name = "training"
urlpatterns = [
    path("", views.home, name="home"),
    path("signup/", views.SignUp.as_view(), name="signup"),
    path("activate/<uidb64>/<token>/", views.activate_account, name="activate-account"),
    path("activation-sent/", views.activation_sent, name="activation-sent"),
    path("profile/", views.Profile.as_view(), name="profile"),
    path("membership/", views.MembershipDashboard.as_view(), name="membership"),
    path("membership/add-balance/", views.add_balance, name="add-balance"),
    path("membership/plans/<int:pk>/buy/", views.purchase_membership, name="purchase-membership"),
    path("vision/", views.VisionDashboard.as_view(), name="vision-dashboard"),
    path("api/vision/analyses/", views.ingest_vision_analysis, name="vision-ingest"),
    path("api/vision/analyses/<int:pk>/snapshot/", views.upload_vision_snapshot, name="vision-snapshot"),
    path("api/vision/live-frame/", views.ingest_vision_live_frame, name="vision-live-ingest"),
    path("api/vision/devices/<int:pk>/live/", views.vision_live_frame, name="vision-live-frame"),
    path("vision/analyses/<int:pk>/snapshot/", views.vision_snapshot, name="vision-snapshot-view"),
    path("earnings/", views.TrainerEarnings.as_view(), name="trainer-earnings"),
    path("payouts/", views.PayoutList.as_view(), name="payout-list"),
    path("reception/", views.ReceptionSessionList.as_view(), name="reception-session-list"),
    path("reception/sessions/<int:pk>/", views.ReceptionCheckIn.as_view(), name="reception-check-in"),
    path("reception/sessions/<int:session_pk>/clients/<int:user_pk>/check-in/", views.kiosk_check_in, name="kiosk-check-in"),
    path("reception/sessions/<int:session_pk>/clients/<int:user_pk>/undo-check-in/", views.kiosk_undo_check_in, name="kiosk-undo-check-in"),
    path("sessions/", views.SessionList.as_view(), name="session-list"),
    path("sessions/create/", views.SessionCreate.as_view(), name="session-create"),
    path("schedules/", views.RecurringScheduleList.as_view(), name="recurring-schedule-list"),
    path("schedules/create/", views.RecurringScheduleCreate.as_view(), name="recurring-schedule-create"),
    path("schedules/<int:pk>/update/", views.RecurringScheduleUpdate.as_view(), name="recurring-schedule-update"),
    path("schedules/<int:pk>/delete/", views.RecurringScheduleDelete.as_view(), name="recurring-schedule-delete"),
    path("sessions/<int:pk>/update/", views.SessionUpdate.as_view(), name="session-update"),
    path("sessions/<int:pk>/delete/", views.SessionDelete.as_view(), name="session-delete"),
    path("sessions/<int:pk>/complete/", views.complete_session, name="session-complete"),
    path("trainers/", views.TrainerList.as_view(), name="trainer-list"),
    path("trainers/create/", views.TrainerCreate.as_view(), name="trainer-create"),
    path("trainers/<int:pk>/update/", views.TrainerUpdate.as_view(), name="trainer-update"),
    path("trainers/<int:pk>/delete/", views.TrainerDelete.as_view(), name="trainer-delete"),
    path("specializations/", views.SpecializationList.as_view(), name="specialization-list"),
    path("clients/", views.ClientList.as_view(), name="client-list"),
    path("clients/<int:pk>/adjust-balance/", views.adjust_client_balance, name="adjust-client-balance"),
    path("specializations/create/", views.SpecializationCreate.as_view(), name="specialization-create"),
    path("specializations/<int:pk>/update/", views.SpecializationUpdate.as_view(), name="specialization-update"),
    path("specializations/<int:pk>/delete/", views.SpecializationDelete.as_view(), name="specialization-delete"),
    path("sessions/<int:pk>/", views.SessionDetail.as_view(), name="session-detail"),
    path("sessions/<int:pk>/book/", views.book, name="book"),
    path("sessions/<int:pk>/cancel/", views.cancel, name="cancel"),
    path("sessions/<int:session_pk>/attendance/<int:user_pk>/", views.update_attendance, name="attendance-update"),
]
